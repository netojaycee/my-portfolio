// Feature flag: real project screenshots haven't been curated for every
// project yet. Set this back to `true` once they have — ProjectCard and the
// case-study gallery will go back to showing real images (falling back to
// the terminal-style ProjectPreview only for a project still missing one).
export const SHOW_PROJECT_IMAGES = false;
